interface SkillsCardProps {
    icon: React.ElementType;
    title: string;
}

interface ProjectCardProps {
    icons: React.ElementType[];
    title: string;
    description: string
    image: string
    grayscale: string
}